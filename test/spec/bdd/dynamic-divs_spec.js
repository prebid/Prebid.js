import assert from 'assert';

// const assert = require('assert');

describe(`Feature: Dynamic Divs`, () => {
  describe(`Given a page has two ad slots with the same adUnitCode`, () => {
    describe('When a request for bids is made', () => {
      it('Then should party!', () => {
        assert.ok(true, 'true');
      });
    });
  });
});
